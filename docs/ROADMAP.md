# Deal-Hunter Roadmap

> 这份文件是项目的长期记忆，取代 README 里的勾选清单。
> 每个 Milestone 都必须能实际验收；做完立刻回来更新，不要让它变成愿望清单。

## Vision

跑在自家服务器上的羊毛侦察兵：宽采集、狠去重、可解释评分，**一天一份日报**。
单二进制、零第三方依赖、无数据库、默认不对外网暴露。

## Current State

- 交付形态已从「发现即推的雷达」改为三类消息：每天一份日报、高分当天最多插队一条、限时事件开抢前提醒（M2/M3）。
- 采集、评分、官方链接校准、近似去重、投递后端（飞书卡片 + OpenClaw drop/relay + console）、只读面板均在位且有离线测试。
- 门禁是本机与办公室构建机上的脚本；**不使用 GitHub Actions**。
- 部署形态仍是一个 systemd 单元 + `/var/lib/deal-hunter` 状态目录（`deploy/install.sh`）。

## Current Milestone

**M3 · 本地消费券与开抢前提醒** —— 设计见
[superpowers/specs/2026-09-20-voucher-event-reminders-design.md](superpowers/specs/2026-09-20-voucher-event-reminders-design.md)。
代码、部署与真机观察都已完成：采集器 `follow_detail` 与列表行日期、`starts_at` 解析、
限时事件的日报在效口径分叉、事件提醒通道（配置 / 调度 / 三条渲染路径 / 面板与状态字段）、
`dealhunter events` 复核命令、南昌两源经 `extra_sources` 上线（17/17 启用）。

**只剩两件**：

1. 真实复核"到时候收到且只收到一次"（开抢与到期共用同一条通道、同一个每行标记）。
   9-21 那天的判断"生产库里 0 行带 `expires_at` / `starts_at`，所以无从触发"**已经过时**：`reparse`
   把当期解析器的结论补回旧行后（09-21 晚的读数：跟踪 10 条、20 行带时刻 = 17 截止 + 3 开抢；
   **这两个数每天在变，取数命令就在下面，别拿本文当读数**），
   最近的一场是 `09-27 23:59 截止`（93 分，到期窗口 = 北京 09-27 20:59 起），其后 `09-30 23:59`
   （80 / 77 / 76 分三条同刻）与 `10-20 06:00 开抢`（63 分）。
   也就是说这件不再是"等一条现实公告"，而是**等这两个时刻**——到期窗口 = 北京 09-30 20:59 起，
   开抢窗口 = 北京 10-20 05:15–06:15；到点各查一次即可，判据与取数命令在 STATUS 的「复核判据」。
   触发路径本身已由夹具与真实措辞两侧证明，今天仍未观察到的只是**窗口内的真实投递**。
2. 区县栏目按「信源准入」流程逐条实测后再加（可达 + 编码 + 列表结构 + 含券样本）。


## Completed Milestones

| # | 内容 | 落点 |
|---|---|---|
| M0 | 全网羊毛雷达初版：自主采集 + 置信评分 + 飞书/OpenClaw 双投递 | `723a480` |
| M1 | 官方链接回查与校验（三级判定 + 预算 + 7 天缓存）、近似标题折叠、只读面板 | `9998ef4`…`5ed7310` |
| M2 | **一天一份日报**：09:00 日报为唯一排程消息，高分当天最多插队一条；空库也发；修复新装上日报永不触发的死锁 | `80721b3`…`8f3e164`，已部署 `alienware-life` |
| M3（代码与部署，实地复核挂起） | 采集 `follow_detail` / 列表行日期；`starts_at` 解析；限时事件不再享受"无期限即在效"；事件提醒通道；`dealhunter events`；南昌两源上线 | `121e14d`…`e849a0b`，已部署 `alienware-life` |
| M4 | 采集轮进行中不再锁住只读面板：roundMu 串行化轮次，mu 只保护运行历史 | `396543f` |
| M5 | 到期前提醒复用同一张 ⏰ 卡（`expires_at` 侧）：`Store.Expiring`、`notify.event.expiry_lead`、两个 due 计数、只提醒未过期的截止；面板日间/夜间配色切换 | `00fc6fb`…`9e21744`，已部署 |
| M6（门禁加固） | `secretlint` 新增 `consumer_mailbox`：个人邮箱写进**文档正文**也会被拦（`check-identity` 只管提交作者），豁免沿用同行 `secretlint:ignore` + 理由 | `25afd4c` |
| M7（备份与恢复） | `deploy/backup.sh`：打包状态与配置后**自解一次做对比**（行数 / config 在位 / sha256），`umask 077` 因为归档里含 env 密钥；`deal-hunter-backup.{service,timer}` 每晚 04:30 且 `Persistent`；恢复路径写进 README 并在生产演练过 | `e23a59f`…`f1e47e4`，已部署 |
| M8（去重与去缝） | 读者时钟只剩顶层 `timezone` 一处，且写错在启动时就拒绝；`notify.DealsOf` 这个只为测试存在的接缝删掉（`m.Deals` 本来就是导出字段） | `838ce9d` |
| M9（语料回灌） | 拿生产库 1040 行真实公告文本喂回解析器量覆盖率：读出截止从 5 行提到 24 行，补的是年份明写的 `开放至/持续至/顺延至…` 与"年 + 前"后缀；配了反向守卫防把发布日读成截止 | `3991677` |
| M10（解析补全） | 无年份的截止按公告自身日期补年（年底附近才允许滚一年，上限 45 天）；日期后紧贴的点被采信（`凌晨5:40` 不再按 23:59 收尾），逗号后的另一句不会被盗用 | `525465f`，已部署 |
| M11（追溯应用） | `dealhunter reparse`：用当前解析器重读已入库行的开抢/截止（游标只往前走，修复追不上旧行）。生产实测 725 条里只有 1 行带 `expires_at`，而当期解析器读得出 14 行 → 已全部写回，跟踪事件 2 → 8，`daily -dry` 里 89 分那条开始显示"截止 09-30"。只补时刻不重算分数；读不出时保留原值。顺带修掉 `StartsAt` 把"截止到9月20日5:40"当成开抢、以及交叉编译产物不带版本戳两处缺陷 | `9103618`…`4905a74`，已部署 |
| M12（时间表达补全） | 两条新读法：**明说的时长**（`限时两周 / 限免 3 天 / 为期两周`，从公告自身日期起算）与**同月缩写区间**（`9月20日至27日` 按前一个日期补月）。三条限制防误伤：只有 限时/限免/为期 算数（`试用一个月`、`3天会员卡` 说的是别的东西的期限）、句中出现任何写全的日期就整个让路、汉字数字只认单字（`三十天` 认不出就不读，不读成"十天"）；区间尾部日期不比开头大也不认。生产第一条 93 分从"无截止"变成 `截止 09-27` 并进跟踪 | `f03ee21`…`881717b`，已部署 |
| M13（维护与闸门） | `Compact` 顺带删掉"所属行已不在"的 `official:deal:<fp>` 与退役的 `digest:*`（TTL 只在读时判，键本身从不删，而 `state.json` 每次整体重写）；`doctor` 单独报排程压缩有没有真跑过——轮次是进程内计数，频繁部署的机器上永远凑不满 48 轮；`ci-office.sh` 的载荷断言改走 here-string（`tar \| grep -q` 在 pipefail 下被 SIGPIPE 打成 141，和健康缺文件长得一样） | `6527ef6`…`7d88af2`，已部署 |
| M14（投递精度） | 日报口径的 `Claimable()` 补上**向人求助**这一族（`求解答 / 求指导 / 请教 / 求推荐 / 帮我看看`）：当天 617 条在效候选里新增拒收 6 条，全是读者点开领不到的（85 分的"不懂，求佬解答"、83 分的"请教推荐哪个plan套餐"、"精装修交付装修请教"…）。中文子串坑：裸 `请教` 会打掉"免费申**请教**程"（申请+教程，恰是真可领的学生许可证条目），故要求前面不是"申"，并把这两条钉成活的对照组 | `7b475ee`，已部署 |
| M15（维护可见性） | `backup.sh` 成功后在状态目录留 `backup.stamp`（时刻 + 归档名，0640 给服务账户，不含密钥），`doctor` 新增 `backup` 一行回答**"上一次真正备份成功是多久前"**——定时器失败只进 journal，没人翻就等于"一直在备份"；顺带修掉**手动 `compact` 不写 `maint:last_compact`**（那条 warn 因此会撒一次谎：生产今晚会没记号是因为备份早于标记，不是没排程），键名收进 `store.StateLastCompact` 一处；另一件被查出来的贵事：跑 `doctor` 的测试漏了 `-net=false`，而 `-net` 默认 true → 每次真实探测 15 个源、单跑 106 秒，三条新测试在门禁里挂满 600 秒超时；全改离线后 cmd 包回到 0.15 秒 | `f5b84f1`…`dc977a4`，已部署 |
| M16（信源静默可见性） | 每轮把"这个源解析出了 ≥1 行"记进 `src:last_hit`，读侧 `SourceIdle`：面板写"上次出声 N 小时前"（>36h 标红点），`/api/v1/sources` 带 `idle_hours`+`last_hit`，`doctor` 新增 `source silence`。记的是**解析出行**而不是"有新发现"，所以低频源几天没新品不会被冤枉。上线当场暴露两个语义洞并修掉：负 Idle 同时表示"从没出声"与"时钟倒挂"（哨兵改成 `Last` 为零、倒挂钳到 0），"从没出声"缺参照点（第一轮无论有无收获都写 `src:last_hit_since`，起点不足 36h 只报"观察中"）。**第二次补正**：`openrouter` 与 `snapshot` 在关键词闸门之后还有自己的一层差分（新增免费模型 / 价格事实有变），`deal()` 只数得到差分后剩下的，于是生产把两个健康源显示成"从没解析出内容"；改为 `base.countSeen()` 数**看到的**行数（目录 443 条 / 事实 8 条），交付 0 条只是"今天没有新东西" | `5fa7c62`…`121a027`，已部署 |
| M17（两个进程各写整份状态） | `PutState` 以磁盘为底再叠加本次写入：服务常驻时，CLI 写的键会被服务用它的旧内存刷掉——生产真实丢过 `maint:last_compact`，doctor 于是谎报"从未压缩过"。退役命名空间改在**装载时**丢弃（`retiredStatePrefixes`），否则一次无关写入就把刚剪掉的键带回磁盘。生产验证：修好后 CLI 写的记号活过了随后 6 轮服务写入。**同一提交里两处规则还互相打过架**： 装载时丢退役键、写入时以磁盘为底合并 —— 合并又把它从磁盘捞回来，于是 `digest:last_sent` 在重启+一轮之后仍躺在文件里；改成两侧都过滤（各配一次变异才证明都不可省）， 而那条测试原本"重新 Open 再 GetState"，被同一个过滤遮住，绿着讲了个假故事 —— 断言要读文件字节 | `e1895c6`…本次提交，已部署 |
| M18（配色三态与空库读数） | 日/夜间切换从两态改成 `跟随系统 → 日间 → 夜间 → 跟随系统`：旧写法点一次就把显式 `light`/`dark` 写进 localStorage，**"跟随系统"再也回不去**，傍晚系统转深色而面板不动。自动态用 `removeItem` 撤销，首屏与点击共用同一条判定（两处各写一份就会开始互相不同意），另挂 `matchMedia` 的 `change` 监听，且**只有自动态**听系统。按钮文案改成写"你现在是哪种"，因为自动态下写"点击切到日间"而系统本来就是日间时，看起来像按钮坏了。顺手在浏览器里看浅色态时抓到一条：空库时 `/api/v1/status` 的 `oldest`/`newest` 是 `0001-01-01T00:00:00Z`，面板原样打印"最近 1/1/1 08:05:43"，现在零值给空串、那行显示"最近 —"。8 条结构守卫逐个变异复查（每个只红一条），三态循环与"不刷新就跟着系统转暗"用 CDP 在无头浏览器里逐点量过 `getComputedStyle` 的实际底色 | 本次提交，已部署 |
| M19（排程压缩不再被重启清零） | 触发器原来是 `rounds % 48`，而 `rounds` 是**进程内**计数：30 分钟一轮要连续 24 小时不重启才凑得满，天天部署的机器上它永远不触发——这就是"`maint:last_compact` 在生产不存在"的成因。现在每轮都进 `compact()`，节流只看盘上记号（距上次 ≥7 天才真剪），重启不再影响。顺带补上 `internal/scheduler` 的**第一份**测试（此前一行测试都没有）：四条守卫各配一次定向变异（去掉节流 / 去掉首轮检查 / 剪完不写记号 / 记号读不出当作刚剪过），每条只红该红的那一条。`doctor` 那行的措辞与 Known Risks 同条一起改口：不再需要外部 systemd timer，代价是**服务从此会自己删数据**（60 天前且分数<60 或已推送的行），生产第一次自动剪枝不早于 `2026-09-28 16:45Z`（北京 09-29 00:45），前一晚有夜间备份 | 本次提交，已部署 |
| M20（解析漂移变成常驻读数） | `doctor` 新增 `reparse drift` 一行：当前解析器能从已入库行里多读出/纠正多少条时刻，就点名多少行。今晚手算过一次（生产 872 行、`expires_at` 18、`starts_at` 3、漂移 0），而这种数本来不该靠人记得去算 —— 游标只往前走，误判过的公告只有 `reparse` 会读第二次。测试钉住 **doctor 的数与 `reparse` dry-run 的数必须一致**。`cmdReparse` 的逐行判定抽成 `reparseRow` 给 CLI 与 doctor 共用（原五条 reparse 测试一字未改仍通过）。**明确不做的部分**：不把写回接进排程（M19 那种"重启清零"的病在这里不存在，因为写回本来就是人的决定——覆盖一条已写明的截止可能把真值改错，与补空字段不同级） | 本次提交，已部署 |
| M21（死代码与从没跑过的支路） | 覆盖率横扫（`-coverpkg` 全量插桩）列出 23 个 0% 函数，逐个查引用：删掉 **7 个没有任何调用者的函数**（`discountPct` 恒等函数、`notify.min` 已被 Go 内建取代、`Feishu.WebhookHost`、`OfficialHosts`、`Deal.Kinds`、`FileDrop.Dir`、`base.IsOfficial` —— Source 接口不含它，管线走 `IsOfficialURL`），删后全量测试仍绿即证明无人依赖。剩下真正值得补测的是两条**配置了才会走、从来没被测过**的采集支路：`snapshot mode=json`（`flattenJSON` 把嵌套价格字段拍平成 `path=value`）与 `html class_contains`（`focusOnClass` 保留每个命中起 4000 字节的窗口 —— 它挡的是窗口外的页头页尾，不是"只留这个元素"）。各配三次定向变异，全部红在该红的那条断言上。顺带补上 CLI 侧一直缺失的承诺：doctor 只准打主机名，不许把 webhook token/签名密钥打到终端（API 侧早就有同样的测试）。以及一个被测试暴露的产品缺陷：`compact()` 用 `_ =` 吞掉记号写入失败，"剪过但没记号"只能从症状反推 —— 现在会 Warn。 | 本次提交，已部署 |
| M22（门禁真的起一次监听） | 接口测试全走 `httptest`，只到 handler 为止 —— **`Serve()` 本身（绑定、`enabled` 闸门、公网绑定拒绝、`-hold` 到点退出）从来没有被任何自动化步骤跑过**。门禁新增一步：临时配置 `bind=127.0.0.1:0`（端口由内核挑，从日志里读回来，不猜固定端口）→ 四个端点必须 200 → 面板 HTML 里 `data-theme` 计数与 `live_in_briefing` 字段在位 → 公网绑定必须在监听之前被拒 → 到点自己停且日志无 ERROR。bash 与 PowerShell 两份孪生脚本各写一遍，并各自做过产品级反向对照：把 `Serve` 改成不监听 ⇒ "never reported a listening URL"；把 `checkBind` 的公网拒绝改成 `return nil` ⇒ "a public bind started without complaint"（bash 那份另用 `DH_ALLOW_PUBLIC_BIND=1` 验过同一臂）。两处差异是环境逼出来的、并写在注释里：PowerShell 的 `Start-Process -PassThru` 拿不到 `ExitCode`（实测只有 `-Wait` 有，而 `-Wait` 期间没法探测），所以"干净退出"这一半只在 bash 断言，PS 断言可观测的那一半（到点不再应答 + 无 ERROR）。 | 本次提交 |
| M23（三份命令清单对表） | 同一件事写在三处：`dispatch` 表（真来源）、`usage` 文本、README 的命令清单。原先只有一条手抄断言（`for _, want := range []string{"events","reparse"}`）——**手抄清单正是会绿着空转的那种**：加一个命令而忘了写文档，它一个字都不会说。现在改成从 AST 读回 `dispatch` 的键，两个方向对表：每个真命令必须同时出现在 usage 与 README 里，文档里也不许出现表里没有的名字。**第一次跑就抓到一条真漂移**：README 少了 `version`（usage 有）。四条反向对照各红该红那条（只进表 / usage 少写 / README 少写 / 文档多出幽灵命令），并删掉了原来那两条手抄名字。 | 本次提交 |
| M24（文档里的旗标也要对表） | 接 M23 的同一形状：文档与门禁脚本里写的 `dealhunter <命令> -旗标` 也是主张，此前没人核对过。测试把 main.go 里**每个 FlagSet 声明的旗标**读回来（按 `flag.NewFlagSet(「x」)` 定位、扫到下一个函数头为止），再要求文档里出现的每个旗标要么属于那条命令、要么是全局旗标；判决会打出该命令实际声明的旗标清单。**中途发现自己写的门禁有个盲区**：命令的识别最初用「有没有 FlagSet」，于是 `serve`/`sources`/`events`/`notify-test`/`version` 这些**没有旗标**的命令整行看不见 —— 而 `-config`/`-data` 恰恰跟在 `events` 后面（M21 那条笔记修的就是它）。改成从 dispatch 表取命令集合后，样本从 7 处涨到 9 处，新增两条正是那对。三次反向对照：真调用行上写 `-nett` → 红（并列出 doctor 声明的 [net]）；产品把 `-net` 改名 → 红；把全局 FlagSet 改名 → 红在「防空转」那道守卫上。第一次对照我故意做**存活**了：把假旗标写在命令清单的描述行里 —— 那不是这条门禁声明的范围，换到真调用行后才红。 | 本次提交 |
| M25（出厂配置只用真存在的键） | 出厂的两份配置（example / starter）是新用户抄的那一份。键名在结构体上改了而它没跟，抄的人就得到一个"写了但不生效"的设置，而且**没有任何一处会报错** —— ROADMAP 里记着这个失败模式。测试用反射把 `Config` 接受的键路径列出来（`json:"-"` 故意不在内），再逐路径核对两份出厂配置，并要求 `Validate()` 通过。今天核对的结果是**没有漂移**（除 `_comment` 这个有意的注释键），所以这条的价值在将来；顺带核了 `deploy/deal-hunter.service` 与生产在跑的单元**逐字一致**。四条反向对照：例子里写 `commnd` → 红并打出 `notify.openclaw.commnd`；写 `webhook_url` → 红（密钥形状的键本就不该出现在配置文件里）；把结构体 tag 改成 `expires_lead` → 两份配置同时红在同一路径；把真来源读错类型 → 红在「防空转」守卫（打出只读回 18 个键）。 | 本次提交 |
| M26（出厂 env 样例与代码双向对表） | 样例是运维照抄的那一份，两个方向都会骗人：代码新增 `DH_*` 而样例没有，功能等于不存在；样例留着一个代码不认的 `DH_*`，照抄的人调半天没反应（`DH_MIN_SCORE` 就是这么被删掉的）。真来源是 config.go 里的常量，测试按它对表。**当场抓到三条缺口**：`DH_FEISHU_API_BASE`（私有化飞书的接口地址，用户根本看不见）、`DH_CONFIG` 与 `DH_ENV_FILE`（引导用，写进 env 文件本身没有意义 —— 补成注释并说明为什么）。规则中途被自己的反例纠正一次：`DH_MIN_SCORE` 只活在"这开关曾经没用"的说明文字里，第一版把它误报成缺口 ⇒ "提到"收紧成**赋值（含注释掉的赋值）**才算把开关交给抄写的人。三条反向对照：删掉一个真赋值 → 红；留一个代码不认的赋值 → 红；**把赋值删掉只留散文提及 → 仍然红**（证明收紧没变成后门）。同一轮还核了 `deploy/deal-hunter.service` 与生产在跑的单元逐字一致。 | 本次提交 |
| M27（采集器种类三处对表） | "有几种采集器"写在四处：`config` 的 `Kind*` 常量（真来源）、`sources.New` 的 switch、README 的表、以及 ROADMAP 那句"采集器类型有 7 种"。而**已有的 kind 测试用的是手抄常量列表**，加一种不会有人报警。新测试从 config.go 读回常量集合，再要求三处都跟得上：每种都能 `New` 出来且 `Kind()` 回原值、README 表里有那一行、ROADMAP 的数字等于实际数目。今天核对**没有漂移**（7 种一致），所以这条是防将来的；ROADMAP 那条技术债（"再加两类以上时值得回到注册表写法"）现在有了自动看门狗。四条反向对照：新增 `KindGraphQL` 不接线 → **一次点亮三条臂**；ROADMAP 数字改成 8 → 只红在数目；README 把 `hn` 改错 → 只红在表；真来源读不到 → 红在防空转守卫（打出 "only 0 Kind constants"） | 本次提交 |
| M28（markdown 表格形状门禁） | 今晚我自己把表格改坏两次：一次在里程碑表中间留了空行（表格断成两截），一次把某行的第三列整个丢掉；上一轮还发现 M16/M17 两行被物理换行切成多段（表格从那儿断掉）。`grep '^\|'` 三种都看不见。新增 `TestMarkdownTablesAreWellFormed`：按 GFM 逐块核对表头 / 分隔行 / 每行的**未转义**竖线数（`\|` 算内容不算列），并拒绝"只有一行的表格块"。当前仓库 12 个表格块全部合规。两条反向对照就是重放我犯过的错：表格中间插空行 → 红并给出行号；某行少一列 → 红并打出"3 个竖线，表头是 4 个" | 本次提交 |
| M29（没有近期成功备份就不许剪） | M19 让排程真的会删数据了，于是补上它的前置：`deploy/backup.sh` 的 `backup.stamp` 必须存在、可读、且在 36 小时以内，否则本轮跳过压缩并 Warn 出原因（缺记号 / 空记号 / 记号读不出 / 太老，四种分开）。方向是刻意保守的：**少剪一次可以补，多删一次救不回**。36 小时这条线原来在 doctor 里手写一份、排程再写一份就成两处主张，所以收进 `store.BackupAge` + `store.BackupPatience`（`ErrNoBackupRecord` 区分「从未跑过」与「记号坏了」，让 doctor 能给出两种不同的下一步）。手动 `dealhunter compact` 不受此限 —— 那是人明确下的令。五条表驱动子测试（四条拒绝 + 一条「两小时前的备份必须真剪」作对照）加四条反向对照：门整段失效、耐心线比反、缺文件当成刚备份过、哨兵错误换成普通错误 | 本次提交，已部署 |
| M30（备份脚本进 CI，含它的破坏性边界） | 压缩排程现在以「36 小时内有成功备份」为前置，于是 `deploy/backup.sh` 站上了一个删数据特性的关键路径 —— 而它此前只在生产上跑过，没有任何自动化碰过它。给它加一个假根（`DH_BACKUP_ROOT`，默认 `/`，归档内部布局与生产一致）并让 root 只在默认路径下才要求，于是 CI 能在 `/tmp` 里用同一份脚本跑完整演练：解得开、行数对得上、config.json 在位、**归档本身 0600**、轮换按 KEEP 只留最新两份且 sidecar 跟着删、`backup.stamp` 写出来后 `doctor` 认得（✓ backup）。**顺手抓到一条破坏性边界**：`DH_BACKUP_KEEP=0` 会让 `head -n -0` 把目录里每个归档都列进删除名单 —— 一次误设就清空全部备份；非数字则静默不剪。现在两种都在动手前 die。四条反向对照：删掉 KEEP 守卫 → 红；`umask 077`→`022` → 红在归档权限（**这条一开始是存活的**，因为我先写的是「归档里 env 文件的模式」，那测的是源文件而不是含密钥的包本身，改成断归档自己的 mode 才咬住）；轮换不删 sidecar → 红；不写 stamp → 红两条。演练在 Windows 上会**明说跳过**（`install -d -m` 在 MINGW 根本设不了 POSIX 权限，那是主机事实），Linux 侧由 ci-office.sh 执行 | 本次提交 |
| M31（备份轮换按修改时间认新旧） | M30 自己埋的雷：那次把 `STAMP` 写成了 `%Y-%m%d`，归档换了格式，而轮换是按名字 `sort` 后删前面的 —— `-`(0x2D) 排在 `0`(0x30) 之前，所以**新格式那份会被当成最旧的删掉**，即最新、也是 `backup.stamp` 正指着的那一份。生产 12 份 / KEEP=14，再跑三次就会动手。现在改成 `ls -1t`（按 mtime）+ 名字回到 `%Y%m%d`：新旧是量出来的，不是从名字里读出来的。演练加两条：归档名形状必须是 `deal-hunter-<8位日期>-<6位时刻>`；再种一个**名字排在最后、mtime 三天前**的诱饵，KEEP=1 跑完后剩下的那份要过自己的 sha256（红在"留下的就是诱饵"）。基线 19 条 ok，两次变异各打死对应那一条（换回 `%Y-%m%d` 只红名字形状；换回按名字排序红两条）。生产侧部署后实跑：单斜杠路径、`20260922-171659` 这个名字、1002/1002 行可解出，并把两种读法的分歧当场量出来（mtime 第一 vs 名字第一） | 本次提交，已部署 |
| M32（部署不再每次留下一份 8.5MB 旧二进制） | 生产 `/opt/deal-hunter` 攒到 227MB：`install.sh` 每次 `cp -a` 一份带时间戳的旧二进制（8.5MB × 28），`/etc/deal-hunter` 同样攒了 28 份配置副本，而没有任何地方回收。二进制改成 `mv` 到固定名 `deal-hunter.bak-prev`——只留上一版，"这是哪一版"由它自己的 `version` 回答（构建戳在里面），不必写在文件名上；再往前是从 git 重建而不是从这块盘上捞。名字仍带 `.bak-` 前缀是有意的：`backup.sh` 的归档按 `*.bak-*` 排除它们，换个名字就等于每个归档多塞 8.5MB。配置改成**一天一份**（`config.json.bak-<日期>`）：生产配置刻意不进 git，这份历史是唯一记录，所以留着，但不留同一个下午的 28 份。旧式遗留**不替人删**（宿主的决定），装完报数量并给一条清理命令。验证走真实路径（门禁覆盖不到 install.sh）：同一份字节连跑两次，第一次 227→235MB、副本 27→28，第二次**一个字节都不再涨**；数量核对时差点被自己骗一次——`warn` 报 27 而宽 glob 数到 29，多出来的两个是今天的日期快照和 `config.json.bak-relay`（openclaw 中继手工留的），说明那条 glob 只数"每次部署多一份"这一类，是对的 | 本次提交，已部署 |

## Next Candidates（按价值排序，未开工）

1. **区县/省级栏目按「信源准入」流程实测后再加**（M3 已给出做法与判据；今天量过的市级栏目
   不含券样本，见下表）。
   2026-09-21 按市政府站内可发现的栏目量过一轮：`bmdt`（部门动态）/ `jrnc`（今日南昌）/
   `gwyxx`（政务信息）三个列表页都 200 / 32KB / 15 行带日期，结构与已接入的 `tzgg` 相同
   （采集器不用改就能解析），但**都不含消费券样本**，`bmdt` 标题抽样全是"召开…推进会 /
   普法宣传活动 / 政府开放周"这类机构动态。按「必须见到含券样本才准入」这条规矩，它们今天
   **不合格**，接进来只会让日报变长。真正的消费券公告就落在已经接入的 `tzgg`（商务局 + 市政府），
   而市级页面里出现的"东湖 / 西湖 / 青山湖 / 红谷滩 / 南昌县 / 新建区 / 安义"是**公告标题里的区县名**，
   不是可跳转的区县站链接 —— 区县商务局页面不在这两台主机的链接图里，要靠猜 URL 才能拿到，
   本项目不猜。等有真实活动跑起来时再量一次即可。

## 已实测否决的方向（别再试第二遍）

- **「按标题长前缀折叠日报里的重复条目」**（2026-09-21 量过，**不能做**）：诱因是真的——
  `AgentCloak 推出免费浏览器扩展…` 两条同功能公告（aihot.news 两个相邻条目 id）占了 15 个槽里的 1 个。
  但在当天 616 条在效候选的高分前 40 名里，只有 2 对共享 ≥12 字前缀，另一对是
  `Qwen3.8-Flash 限时免费一周`（公告，93 分）与 `Qwen3.8-Flash 限时免费？`（**提问帖**）——
  前缀 18 字却完全不是同一件事。任何"前缀够长就算重复"的规则都会把后者这类"同名不同事"合并掉，
  而价格差一个数字的条目（`首年 99 元` / `首年 199 元`）差异恰恰在尾部。
  代价 1/15 槽，收益不抵误伤，不做。

- **「近似标题折叠会让新一期公告被过期的占位行吞掉」**（2026-09-21 拿生产库量过，**不成立**）：
  折叠规则确实是"先见者拥有标题"，所以理论上存在一个失败模式——同名不同期的公告（如每月同名的消费券）
  里，新那期被挂到已过期老实例的 `dup_of` 上，于是 `Live` 两边都丢、日报和提醒同时看不见它。
  实测 721 条去重记录里 24 组同名多条，全部是正当折叠（v2ex 的 `#replyN` 锚点、`tencent.com` 与
  `tencent.com.cn` 镜像域、跨源转载），**其中没有任何一行带 `expires_at`**，"后行截止晚于占位行"的组数 = 0。
  所以今天不改：改折叠方向（改成最新者拥有）会动到 `event:reminded:<fp>` 与 `pushed` 的归属，代价大于收益。
  复判办法：在生产库上按 `model.DedupKey` 归组，只看那些**组内出现两个以上不同 `expires_at`** 的组——
  有这种组才说明吞掉了有效公告；一条命令即可数出来，别凭规则的形状下结论。

- **「官方目录差分：推理云 / 向量库 / GPU 云」**（2026-09-21 从生产出口实测后否决）：
  OpenAI 兼容的 `/v1/models` 全部要密钥 —— `api.deepseek.com` `api.moonshot.cn` `api.groq.com`
  `api.together.xyz` `api.siliconflow.cn/v1/models` 一律 401，`api.siliconflow.cn/v1/model/list/all`
  404，`api.github.com/models` 404；只有 `openrouter.ai/api/v1/models` 是公开 JSON（200 / 738KB /
  446 个 id / 带 `prompt`、`completion` 每 token 价格），而它已经在采集。**公开目录这条路没有第二个入口。**
  定价页方向同样不通：`siliconflow.cn/pricing`、`bigmodel.cn/pricing`、`autodl.com/pricing`、
  `cloud.zilliz.com.cn/pricing`、`volcengine.com/product/vikingdb` 全部 200，但页面里
  **服务端渲染的价格标记 `¥<数字>` 命中数为 0**，其中三个只有 3–6 KB 的 JS 壳。要读它们得上无头浏览器，
  而本项目是零第三方依赖的单二进制、不跑浏览器，用户的夜间调度也明确要求不拉起可见窗口。
  结论：这类信息只能靠现有的 `search` 采集器从第三方转述里拿（已在跑），或者等官方给出无需鉴权的目录接口。

## Known Risks

- **投递扇出任一成功即算送达**：一天只有一份消息，所以某个通道坏了当天这份就只存在于日志里。
  目前靠 `notify: partially delivered` 警告与 `/api/v1/status` 暴露，不做重试。真需要更强保证时要先定策略（去重 vs 漏投）。
- **备份目前与生产同盘**：`/var/backups/deal-hunter` 在 `alienware-life` 自己的磁盘上，能救误删与
  状态损坏，救不了整机故障。跨机那份要设 `DH_BACKUP_PUSH`，目标机器由用户定（见 STATUS Notes）。
- **同一场赞助活动换措辞重发，会各提醒一次**：事件通道按 `fingerprint` 去重，而近似标题折叠只认
  `DedupKey`（去标点后相同）。2026-09-21 生产里就有实例：两条 `[推广] 《我享云》机场代理…` 分属
  v2ex `t/1243236` 与 `t/1243515`，措辞差在"比矿泉水还便宜"与"一瓶矿泉水/月"，同一截止时刻
  `09-30 23:59`，两行都进了跟踪列表。每天只有 2 条事件预算，重复一条就占掉一半。
  **不做模糊匹配**：两家不同机场的促销也可能只差几个字，误合的代价是漏提醒。要收敛得先有
  "同一活动"的可靠判据（赞助商 + 时刻 + 领取入口），目前没有。
- **`max_age_hours` 只管入库不管在效**：`filter.max_age_hours`（默认 14 天）只在采集时丢弃
  发布已久的行，且要求 `published_at` 已知；`Store.Live` 没有年龄上限。所以一条**没有**写明截止的
  在效条目会一直留在日报里 —— 这是 `reparse` 存在的原因之一，也是解析器读不出截止的真实代价。
- **日报只在采集轮结束后检查**：`daily.at` 到点后最多再等一个 `interval`（默认 30m）才发；发送失败下一轮重试。

- **插队门槛 90 分只有一个真实样本**：2026-09-21 14:19 `Qwen3.8-Flash 限时免费一周` 打出 93 分并
  真实投出（见 STATUS 第 4 条），落在预测的 87–97 带内。但一个样本不足以判断"门槛是不是太高/太低"，
  也没验证过同一天第二条高分被 `held` 的行为。**别再把它当未验证的估值，也别当已定案的阈值。**
- **排程压缩曾经数的是进程内轮次（已修，M19）**：`scheduler` 每 48 轮触发一次 `Compact`，而轮次是
  **进程内计数**——30 分钟一轮意味着要"连续 24 小时不重启"才凑得满，任何一次部署都把它清零。
  2026-09-21 生产跑了一整天、部署六次，`maint:last_compact` 这个键**至今不存在**。
  当晚手动跑过一次 `compact` 以验证新路径：`deals.jsonl` 1115 → 749 行（`749 → 749 条`，一行没丢）。
  2026-09-22 改成**每轮都检查、以盘上的 `maint:last_compact` 节流（≥7 天才真剪）**，
  重启不再影响触发；`scheduler_test.go` 四条守卫逐个变异复查过（去掉节流 / 去掉首轮检查 /
  剪完不写记号 / 记号读不出当作刚剪过，各红对应那条）。
  剩下的运维问题只有一个：**7 天与 60 天这两个数合不合适**（现在生产 868 行 / 1.4 MB，
  一夜 +119 行，按这个速度 7 天一剪足够稳）。
  **注意这条改动的后果**：排程从此真的会自己删数据（60 天前且分数<60 或已推送的行），
  不再需要额外那条 systemd timer（本条到此作废：不必再加外部定时器，服务自己就会做）。
  生产第一次自动剪枝的时刻 = 盘上记号满 7 天，具体日期记在 STATUS。
  **前置（M29 起是硬条件，不再靠"前一晚应该有备份"这个假设）**：`backup.stamp` 必须存在、
  读得出、且在 36 小时以内，否则本轮跳过压缩并 Warn —— 备份定时器坏了宁可让库长一点。
- **状态键的剪枝要等持有它的进程重启才生效**（写这条是因为我先说过头过一次）：`state.json` 由每个
  打开它的进程整份读进内存、再整份写回。所以常驻服务活着时，`compact` 删掉的键会被它下一次写**任何**
  状态时原样带回磁盘 —— 2026-09-21 我写"退役的 `digest:*` 已被清掉"，第二天它就在生产回来了。
  现在两道一起解决：装载时丢掉退役命名空间（`retiredStatePrefixes`，`digest:` 没有任何读者），
  以及 `PutState` 以磁盘为底合并。剩下的含义要写清楚：**剪枝在下一次重启后彻底生效**，别当即时清理。
- **`Compact` 顺带删掉跟随行而死的缓存键**：`official:deal:<fp>` 只在读的时候判 7 天 TTL，
  键本身从不删；行被压缩掉之后那个键再没人读得到，而 `state.json` 每次写入都是整体重写。
  现在压缩会把"所属行已经不在了"的判定键一起删掉（生效时机见上一条）；
  `official:verify:<url>` 跨行共享，保留。

- **升级后旧配置里的废弃键不报错也不生效**（`notify.digest`、`notify.feishu.{min_score,max_per_run,silent_hours}`、`filter.min_score`），`install.sh` 会打一行提示。

## Technical Debt
- **`Store.Live` 每次都整体重读 JSONL —— 量到 10 倍规模了，仍然不是债**：2026-09-21 生产实测
  `daily -dry` 端到端 `0.09 s`（库 1.2 MB / 779 行）。2026-09-22 拿**合成到稳态大小**的库重测
  （7200 行 / 2.5 MB ≈ M19 之后"60 天 × 一夜 120 行"的平衡点）：`/api/v1/status` `0.55–0.69 s`、
  `/api/v1/deals?limit=24` `0.17–0.20 s`、`/api/v1/sources` 与 `/runs` `~2 ms`；同一份二进制换成
  20 行的库，`status` 是 `3–6 ms` —— 所以成本是**每行约 0.08 ms × 每次请求 3 次全扫**
  （`BriefingQuality` 的 `Live` + `Event` 的 `Upcoming` 与 `Expiring`），与行数线性。
  面板 30 秒轮询一次 ≈ 单核 2%，且首屏是异步渲染。**仍然别为此加缓存**：
  服务与 CLI 两个进程都会写这个文件（M17 整条就是为此），缓存一致性是新故障，而现在的成本是几百毫秒。
  复测办法（一次命令）：造 7200 行 `deals.jsonl` → `serve` → `curl -w '%{time_total}'` 三次，
  再拿 20 行的库跑同一条对照 —— 没有对照那一步，2 秒的"连接被拒"超时会被当成慢查询（我这次就先读到了它）。
- ~~`internal/scheduler` 没有测试文件~~ —— **已不成立（M19）**：`scheduler_test.go` 四条守卫，
  不需要抽接口：`Loop` 拿真的 `*pipeline.App`（`pipeline.New` + 桩 fetcher + `t.TempDir()` 数据目录）
  就能跑，触发判断走真存储。原先"要测就得造抽象"的判断是错的。
- 采集器类型有 7 种，`sources.New` 是一个 switch；再加两类以上时值得回到注册表写法。

## Non-Goals

- 不引入数据库、Docker、消息队列、微服务拆分或前端构建链（JSONL + 单二进制是特性不是债）。
- 不跑无头浏览器去读 SPA 定价页：那等于把"零第三方依赖"换成一个 Chrome。这类信息拿不到就是拿不到，
  宁缺毋滥（实测见「已实测否决的方向」）。
- 不用 GitHub Actions 当门禁（额度优先留给代码托管与 Release）。
- 不做多群路由 / 分类订阅：目前只有一个读者。
- 不把 `0.0.0.0` 暴露成公网服务，也不为公网访问写认证层。
